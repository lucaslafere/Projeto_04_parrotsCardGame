const container = document.querySelector(".card-box");
let qtd = prompt("Com quantas cartas deseja jogar?");
while (qtd % 2 !== 0) {
    qtd = prompt("Com quantas cartas deseja jogar?");
}

function qtdCard () {
    for(let i = 0; i < qtd; i++){
        //adicionar card ate i = qtd --- nao sei como fazer add card
    }
}
